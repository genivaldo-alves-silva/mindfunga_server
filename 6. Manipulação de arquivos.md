# 6. Manipulação de arquivos (múltiplos)

Alternativas a:
```bash
# Arquivos selecionados alvo serão copiados. Será pedido a senha de acesso ao servidor a cada arquivo.
scp -r SEU_USUARIO@server.ip.address:/home/SEU_USUARIO/filogenia/**{infile.nex,infile.nex.con.tre,infile.nex.run1.p,infile.nex.run2.p,mrbayes.out}** /home/user/Downloads/filogenia_genusX
```

## 🖥️ Passos para Transferência com SFTP (Mget)
Você deve executar estes passos a partir do terminal do seu PC local (não do servidor via SSH).

## 6.1 Iniciar a Conexão SFTP
Inicie uma sessão SFTP com o servidor. Você será solicitado a digitar a senha ou a passphrase da sua chave SSH:

```bash
sftp SEU_USUARIO@server.ip.address
```

## 6.2 Navegar para o Diretório de Origem (no Servidor)
Uma vez conectado, use o comando cd (change directory) para ir para o diretório que contém os arquivos:

```bash
sftp> cd /home/SEU_USUARIO/filogenia
```

## 6.3 Definir o Diretório de Destino (no PC Local)
Use o comando lcd (local change directory) para definir onde no seu PC local os arquivos serão salvos.

```bash
sftp> lcd /home/user/Downloads/filogenia_genusX
```
Nota: Certifique-se de que este diretório já exista no seu PC local.

## 6.4 Baixar os Arquivos (Usando mget)
Agora, você usará o mget (multiple get) com diferentes coringas (*) para puxar os arquivos:

Opção A: Puxar todos os arquivos .nex
Para puxar os arquivos que terminam em .nex ou possuem .nex no meio:

```bash
sftp> mget *.nex
sftp> mget *.nex.con.tre 
sftp> mget *.nex.run*.p
```

## Opção B: Puxar todos os arquivos com mget e o arquivo mrbayes.out com get
Como o mrbayes.out é um arquivo único e tem um nome diferente, você deve baixá-lo separadamente (ou usando um coringa mais amplo, mas menos específico).

1. Baixar arquivos .nex e afins (com coringas):

```bash
sftp> mget *nex*
```
Isso pode ser mais amplo que seu original e baixar outros arquivos que contenham a string "nex".

2. Baixar o arquivo específico mrbayes.out:

```bash
sftp> get mrbayes.out
```

## 6.5 Sair do SFTP
Após a conclusão das transferências:

```bash
sftp> bye
```

## 6.6 Alternativa: Comando Único (com SCP ou SFTP "inline")
Se você realmente prefere uma única linha de comando não interativa (como o seu scp original), o sftp interativo não é o ideal. Você deve usar a sintaxe scp (executada localmente) ou o comando sftp em modo não interativo:

```bash
# Opção 1: Usando SCP (no PC Local)
scp SEU_USUARIO@server.ip.address:/home/SEU_USUARIO/filogenia/**{infile.nex,infile.nex.con.tre,infile.nex.run1.p,infile.nex.run2.p,mrbayes.out}** /home/user/Downloads/filogenia_genusX

# Opção 2: Usando SFTP em modo não interativo (no PC Local)
# Isso permite que você execute comandos SFTP diretamente:
sftp SEU_USUARIO@server.ip.address:/home/SEU_USUARIO/filogenia/ <<EOF
lcd /home/user/Downloads/filogenia_genusX
mget *nex*
get mrbayes.out
bye
EOF
```

#  6.7 Passo a Passo para Usar o FileZilla (SFTP)

## 6.7.1 Preparação: Baixar e Abrir o FileZilla
Baixe e instale o cliente FileZilla para seu sistema operacional (Windows, macOS ou Linux).

Abra o FileZilla. Você verá três painéis principais: o painel de Status no topo, o painel Local (seu PC) à esquerda e o painel Remoto (o Servidor) à direita.

## 6.7.2 Conectar ao Servidor via SFTP
Você usará a barra de conexão rápida ou o Gerenciador de Sites:

| Campo na Barra de Conexão Rápida | O que inserir | Exemplo |
| :--- | :--- | :--- |
| **Host** | O endereço IP do seu servidor (`server.ip.address`). | `192.168.1.100` |
| **Nome de usuário** | Seu usuário SSH/SFTP (`SEU_USUARIO`). | `maria.silva` |
| **Senha** | Sua senha ou o campo vazio (se usar chaves SSH). | `********` |
| **Porta** | A porta SSH, que geralmente é **22** (essencial para SFTP). | `22` |

1. Preencha os campos e clique no botão "Conexão Rápida".
2. Se você estiver usando chaves SSH e não senha, precisará configurar a chave em Editar > Configurações > SFTP antes de conectar.
3. O FileZilla tentará conectar. Se for a primeira vez, ele pode pedir para confiar na chave do host; clique em "OK".

## 6.7.3 Transferir os Arquivos
Após a conexão, os painéis serão preenchidos:

## A. Navegar para a Origem (Servidor)
No painel Remoto (direita), navegue até o diretório onde os arquivos estão localizados:

```bash
/home/SEU_USUARIO/filogenia/
```

## B. Navegar para o Destino (PC Local)
No painel Local (esquerda), navegue até o diretório onde você quer salvar os arquivos:

```bash
/home/user/Downloads/filogenia_genusX
```

## C. Selecionar e Baixar
1. No painel Remoto (servidor), localize os arquivos que você deseja baixar: infile.nex, infile.nex.con.tre, etc.
2. Selecione todos eles. Você pode clicar no primeiro, segurar Ctrl (Windows/Linux) ou Cmd (macOS) e clicar nos outros.
3. Arraste e Solte os arquivos selecionados do painel Remoto (direita) para o painel Local (esquerda).

O FileZilla iniciará a transferência dos arquivos, e você pode acompanhar o progresso no painel inferior.

## 6.7.4 Desconectar
Quando a transferência estiver concluída, você pode desconectar:

- Clique no ícone de "Desconectar" (parecido com um pino de porta) na barra de ferramentas ou simplesmente feche o programa.

